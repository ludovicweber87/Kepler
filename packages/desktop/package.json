{
	"name": "kepler-desktop",
	"version": "0.1.0",
	"private": true,
	"description": "Electron window wrapping the local Kepler server",
	"main": "src/main.js",
	"scripts": {
		"postinstall": "node scripts/patch-electron-name.js"
	},
	"dependencies": {
		"electron": "^35.0.0"
	}
}
