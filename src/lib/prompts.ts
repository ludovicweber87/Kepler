/** Prompt par défaut envoyé à l'agent par le bouton "Create PR". Éditable dans les settings. */
export const DEFAULT_CREATE_PR_PROMPT =
	'Commit les changements avec des messages clairs, push la branche, puis ouvre une pull request avec un titre et une description qui résument le travail effectué.';

export const CREATE_PR_PROMPT_KEY = 'create_pr_prompt';

/** Prompt par défaut envoyé à l'agent par le bouton "Commit and push". Éditable dans les settings. */
export const DEFAULT_COMMIT_PUSH_PROMPT =
	'Commit tous les changements en cours avec un message clair (convention karma), puis push la branche.';

export const COMMIT_PUSH_PROMPT_KEY = 'commit_push_prompt';
